#include <stdio.h>
#include "main.h"
/**
* print_rev - reverse a string
*@s : pointer to the string to reverse
* Return: void
*/

void print_rev(char *s)
{
int i;
for (i = 0; str[i] != '\0'; i--)
{
_putchar(str[i]);
}
_putchar('\n');
}
