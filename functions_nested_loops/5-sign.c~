#include  <stdio.h>
#include "main.h"
/**
*print_sign - prints the sign of a number
* @n : character that need to be checked
*Return: 0 or 1
*/
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
return (1);
else
return (0);
}
