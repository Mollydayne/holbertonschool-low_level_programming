#include  <stdio.h>
#include "main.h"
/**
*jack_bauer- prints every minute
*
*Return: n or -n
*/
void times_table(void);
int chiffre, multiplicateur, resultat

for (chiffre = 0; i <= 9; i++)
{
_putchar('0');
}
for (multiplicateur = 0; i <=9; i++)
{
_putchar(multiplicateur)
_putchar(' ');
_putchar(',');
}
for (resultat = 0; i <= 9; i++)
_putchar(resultat);
}
_putchar('\n');
}
